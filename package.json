{
  "name": "munshiq-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cd dist && touch .nojekyll && git init -b gh-pages && git add -A && git commit -m Deploy && git push --force https://github.com/swagatm001/munshiq-web.git gh-pages && cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
